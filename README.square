Make sure you set JAVA_HOME. On OS X:

$ export JAVA_HOME=/System/Library/Frameworks/JavaVM.framework/Versions/1.6/Home

To install, see INSTALL. Note: Scala and the mysql jars are checked in, so you
can skip that step.

To build: 

$ ./bin/build.sh

To run:

$ ./bin/run.sh

That's it!
